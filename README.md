# Портфолио группы АУБ-1-24 (ОшГУ)

Полноценный проект на **Next.js 14 + TypeScript** для группы **АУБ-1-24** (Ошский Государственный университет, Институт МФТИТ, кафедра ПИиИБ). Включает публичные страницы, профили студентов, личные кабинеты, админ-панель и интеграцию с Supabase.

## ✨ Возможности

- Публичные страницы: Главная, Студенты, Профиль студента, Новости, Путешествия
- Личный кабинет студента с редактированием профиля, навыков и приватности
- Админ-панель для управления студентами, новостями и поездками
- Анимации Framer Motion, плавные переходы и скролл-эффекты
- Tailwind CSS + компоненты в стиле shadcn/ui
- React Hook Form + Zod
- Supabase Auth + Postgres + Storage

---

## 📁 Структура проекта

```
app/
  layout.tsx
  page.tsx
  students/
  student/[slug]/
  news/
  trips/
  dashboard/
  admin/
  auth/login/
components/
  animated-navbar.tsx
  hero.tsx
  news-card.tsx
  profile-header.tsx
  project-list.tsx
  scroll-reveal.tsx
  stats-counters.tsx
  student-card.tsx
  tag-input.tsx
  theme-toggle.tsx
  trip-gallery-lightbox.tsx
  upload-avatar.tsx
  ui/
lib/
  data.ts
  supabaseClient.ts
  supabaseServer.ts
  utils.ts
supabase/
  schema.sql
  seed.sql
middleware.ts
```

---

## ✅ Как запустить локально

### 1) Создать проект Supabase
- Зарегистрируйтесь на https://supabase.com
- Сохраните **Project URL** и **Anon Key**.

### 2) Настроить переменные окружения
Создайте `.env.local`:

```
NEXT_PUBLIC_SUPABASE_URL=ваш-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-key
```

### 3) Применить SQL-схему + RLS
Выполните SQL из `supabase/schema.sql` в Supabase SQL Editor.

### 4) Загрузить демо-данные (опционально)
Выполните `supabase/seed.sql`.

### 5) Storage buckets
Создайте buckets в Supabase Storage:

- `avatars` (публичное чтение, запись владельцу/админу)
- `trip-media` (запись только админу)

### 6) Установить зависимости

```
npm install
```

### 7) Запустить локально

```
npm run dev
```

---

## 🔑 Демо-логины (создаются в Supabase Auth вручную)

> Важно: для работы авторизации нужно создать пользователей в **Supabase Auth** вручную и
> связать их с таблицей `profiles` через поле `user_id`.

### Админ
- **Логин:** `admin.aub124@oshsu.kg`
- **Временный пароль:** `AdminAUB124!`
- В `app_metadata` у пользователя установите `role: "admin"`.

### Студенты
Логины (email‑формат вида `aub124.имя-фамилия@oshsu.kg`) и временные пароли указаны в админ-панели на странице `/admin` (раздел «Учетные записи студентов»).

---

## 🔐 Политики безопасности (RLS)

- **profiles**: публичное чтение, редактирование только владельцем
- **projects**: публичное чтение, управление только владельцем профиля
- **posts**: публичное чтение только опубликованных, админ управляет всем
- **trips**: публичное чтение только опубликованных, админ управляет всем
- **trip_media**: публичное чтение только для опубликованных поездок
- **storage**: отдельные политики для `avatars` и `trip-media`

---

## ✅ Чеклист тестирования

- [ ] Страницы /, /students, /student/[slug], /news, /trips открываются
- [ ] Фильтры студентов и новости работают
- [ ] Прогресс чтения в новости отображается
- [ ] Личный кабинет сохраняет данные и показывает уведомления
- [ ] Админ-панель отображает логины и дисциплины
- [ ] RLS не даёт доступ к чужим профилям
- [ ] Телефон/email видны только при включенной приватности
- [ ] Медиа поездок доступно только по опубликованным поездкам

---

## 🚀 Деплой на Vercel

1. Запушьте репозиторий на GitHub.
2. Импортируйте проект в https://vercel.com
3. Укажите переменные окружения:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. Нажмите Deploy.

---

**АУБ-1-24** — Ошский Государственный университет · Институт МФТИТ · Кафедра ПИиИБ
